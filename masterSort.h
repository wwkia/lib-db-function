#ifndef MASTERSORT_H
#define MASTERSORT_H  // check syntax

#include <iostream>
#include <cassert>
#include <algorithm>
#include <vector>
#include <string>
#include <fstream>

class Sort() {
	virtual bool operator(const Book& book1, const Book& book2);
};

#endif